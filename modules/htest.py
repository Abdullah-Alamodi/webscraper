from scraper import scraper

# links = scraper.get_haraj_links(url="https://haraj.com/", nu_of_pages=3)

# details = scraper.get_haraj_details(links, save=True, save_format="json")
 
scraper.get_aqar_main("https://sa.aqar.fm", 0)